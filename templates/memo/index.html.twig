{% extends 'base.html.twig' %}

{% block title %}Mémo{% endblock %}

{% block body %}
    <div class="container">
        <h1>Nouveau mémo</h1>
        <div class="row">
            <div class="col-md-3">
                <label for="contenu">Contenu :</label>
                <textarea id="story" name="story" rows="5" cols="33">
                    {# {{memo.contenu}} #}
                </textarea>
                </br>
                <label for="tentacles">Délai d'expiration :</label>

                <input type="number" id="tentacles" name="tentacles" min="1" max="180">
                </br>

                <a type="button" class="btn btn-primary" href="{{ path('memo.test') }}">Créer</a>
                
        

                {# <tbody>
                {% for memo in memos %}
                    <tr>
                        <td>{{memo.contenu}}</td>
                    </tr>
                {% endfor %}
                </tbody> #}

            </div>

    </div>
{% endblock %}
